# Local dev: leave empty (Vite proxies /api → localhost:3000).
# Production builds default to https://gyotaku-api.up.railway.app.
# Optional override:
# VITE_API_URL=https://gyotaku-api.up.railway.app
#
# Runtime (server.mjs /api proxy, optional):
#   API_PROXY_TARGET=https://gyotaku-api.up.railway.app
VITE_API_URL=

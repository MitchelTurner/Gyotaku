# Local dev: leave empty (Vite proxies /api → localhost:3000).
# Railway web service: leave VITE_API_URL EMPTY and set runtime:
#   API_PROXY_TARGET=https://gyotaku-api.up.railway.app
# so the browser calls same-origin /api/* (no CORS).
VITE_API_URL=
